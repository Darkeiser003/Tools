[package]
name = "ltools"
version = "1.0.0"
edition = "2021"
description = "Núcleo nativo y seguro para LTools"
license = "MIT"

[dependencies]
libc = "0.2"
sha2 = "0.10"

[profile.release]
strip = true
lto = true
codegen-units = 1
