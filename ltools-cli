#!/usr/bin/env bash
# Perfil CLI portable con el nombre que publican los descriptores.
set -Eeuo pipefail
SCRIPT_DIR="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd -P)"
exec "$SCRIPT_DIR/ltools-cli.sh" "$@"
