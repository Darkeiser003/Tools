#!/usr/bin/env bash
# Entry point portable con el nombre que publican los descriptores de
# integración. La lógica sigue centralizada en ltools.sh.
set -Eeuo pipefail
SCRIPT_DIR="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd -P)"
exec "$SCRIPT_DIR/ltools.sh" "$@"
